import React from 'react';
import { motion } from 'framer-motion';
import { styles } from '../styles';
import { services } from '../constants';
import { fadeIn, textVariant } from '../utils/motion';
import { SectionWrapper } from '../hoc';

const ServiceCard = ({ index, title, icon }) => {
  return (
    <motion.div
      variants={fadeIn('right', 'spring', 0.5 * index, 0.75)}
      className="xs:w-[250px] w-full card-gradient p-[1px] rounded-[20px] shadow-card">
      <div
        options={{
          max: 45,
          scale: 1,
          speed: 450,
        }}
        className="bg-blue-500 rounded-[20px] py-5 px-12 min-h-[280px] flex justify-evenly items-center flex-col">
        <img src={icon} alt={title} className="w-16 h-16 object-contain" />
        <h3 className="text-white text-[18px] font-bold text-center">
          {title}
        </h3>
      </div>
    </motion.div>
  );
};

const About = () => {
  return (
    <div className="-mt-[6rem]">
      <motion.div variants={textVariant()}>
        <p className={styles.sectionSubText}>Introduction</p>
        <h2 className={styles.sectionHeadText}>Overview.</h2>
      </motion.div>

      <motion.p
        variants={fadeIn('', '', 0.1, 1)}
        className="mt-4 text-taupe text-[18px] max-w-3xl leading-[30px]">
        Welcome to my portfolio! I am a passionate and dedicated MERN (MongoDB, Express.js, React.js, Node.js) stack developer with a strong foundation in web development. With a keen eye for detail and a knack for problem-solving, I strive to create efficient, robust, and user-friendly applications.

        Throughout my journey as a fresher developer, I have acquired a solid understanding of the MERN stack, along with proficiency in HTML, CSS, JavaScript, and various front-end libraries and frameworks. I enjoy the challenge of transforming ideas into functional and visually appealing websites and applications.

        My approach to development is centered around clean code, scalability, and best practices. I am committed to continuously learning and staying updated with the latest industry trends and technologies. This enables me to deliver modern and cutting-edge solutions that meet the evolving needs of users and businesses alike.
      </motion.p>

      <div className="mt-20 flex flex-wrap gap-10">
        {services.map((service, index) => (
          <ServiceCard key={service.title} index={index} {...service} />
        ))}
      </div>
    </div>
  );
};

export default SectionWrapper(About, 'about');
